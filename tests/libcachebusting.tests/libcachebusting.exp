load_lib dejagnu.exp

set dir [pwd]
set prefix $srcdir/$subdir
cd $prefix
print "Switched to directory $prefix"

set tests [glob *.c]
foreach test $tests {
	set executable [string trimright $test ".c"]
	print "Running test: $executable"
	host_execute "./$executable"
}
cd $dir
